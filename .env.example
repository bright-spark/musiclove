# Copy to `.env` locally — never commit `.env`.
# If DATABASE_URL was ever committed or shared, rotate the password in Neon immediately.
DATABASE_URL=postgresql://USER:PASSWORD@HOSTNAME/neondb?sslmode=require
